import type { WorkoutLog } from '../db/CompletionContext';

// Native stub — graph only renders on web (see ProgressGraph.web.tsx)
export default function ProgressGraph(_props: { logs: Map<string, WorkoutLog> }) {
  return null;
}
